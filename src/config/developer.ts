import type { DeveloperConfig } from '@shared-types/config'

export const DEFAULT_DEVELOPER_CONFIG: DeveloperConfig = {
  seedOverride: null,
  verboseLogging: false,
  debugOverlaysEnabled: false,
}
